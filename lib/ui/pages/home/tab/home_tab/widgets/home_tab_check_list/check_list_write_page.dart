import 'package:flutter/material.dart';

class CheckListWritePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('새로운 체크리스트 작성'),
      ),
      body: Text('체크리스트를 작성해주세요.'),
    );
  }
}
